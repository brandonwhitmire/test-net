# Reference: https://wiki.debian.org/DebianInstaller/Preseed
# Reference (from Packer): https://www.packer.io/guides/automatic-operating-system-installs/preseed_ubuntu
# Example: https://github.com/algleason/packer-kali-linux/blob/master/http/preseed.cfg
# Documentation: https://preseed.debian.net/debian-preseed/

# General
d-i debian-installer/language string en
d-i debian-installer/country string US
d-i debian-installer/locale string en_US.UTF-8
d-i clock-setup/utc boolean true
d-i time/zone string US/Central

# Keyboard selection.
d-i console-setup/ask_detect boolean false
d-i keyboard-configuration/xkb-keymap select us

choose-mirror-bin mirror/http/proxy string

# Network mirror (without this, offline/cdrom install leaves apt with no sources)
d-i mirror/country string manual
d-i mirror/http/hostname string http.kali.org
d-i mirror/http/directory string /kali
d-i mirror/suite string kali-rolling
d-i apt-setup/use_mirror boolean true
d-i apt-setup/services-select multiselect
d-i apt-setup/cdrom/set-first boolean false
d-i apt-setup/cdrom/set-next boolean false
d-i apt-setup/cdrom/set-failed boolean false

# This is fairly safe to set, it makes grub install automatically to the MBR
# if no other operating system is detected on the machine.
d-i grub-installer/only_debian boolean true

# Partitioning Disk
d-i partman-auto/method string lvm
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman-auto-lvm/guided_size string max
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm_nooverwrite boolean true
d-i partman/confirm boolean true

# GRUB (bootloader)
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

# Account setup for:
# -	User: vagrant
# -	Pass: vagrant
d-i passwd/user-fullname string vagrant
d-i passwd/username string vagrant
d-i passwd/user-password password vagrant
d-i passwd/user-password-again password vagrant
d-i passwd/user-uid string 1000
d-i user-setup/allow-password-weak boolean true
d-i user-setup/encrypt-home boolean false
d-i passwd/root-login boolean false

# Package selection
tasksel tasksel/first standard
d-i pkgsel/include string build-essential openssh-server vim qemu-guest-agent
d-i pkgsel/install-language-support boolean false

# Enable SSH, sudo, apt sources, and systemd-networkd DHCP on eth0.
d-i preseed/late_command string \
    in-target systemctl enable ssh.service && \
    in-target sh -c "echo 'vagrant ALL=(ALL) NOPASSWD: ALL' | tee /etc/sudoers.d/10-vagrant" && \
    in-target chmod 0440 /etc/sudoers.d/10-vagrant && \
    in-target sh -c "mkdir -p /etc/apt/sources.list.d && printf '%s\n' 'Types: deb' 'URIs: http://http.kali.org/kali/' 'Suites: kali-rolling' 'Components: main contrib non-free non-free-firmware' 'Signed-By: /usr/share/keyrings/kali-archive-keyring.gpg' > /etc/apt/sources.list.d/kali.sources" && \
    in-target sh -c "systemctl enable qemu-guest-agent.service || true" && \
    in-target sh -c "mkdir -p /etc/systemd/network && printf '%s\n' '[Match]' 'Name=eth0' '[Network]' 'DHCP=yes' > /etc/systemd/network/20-mgmt-eth0.network" && \
    in-target systemctl enable systemd-networkd.service && \
    in-target systemctl enable systemd-resolved.service && \
    in-target sh -c "ln -sfn /run/systemd/resolve/stub-resolv.conf /etc/resolv.conf" && \
    in-target sh -c "sed -i -e 's/^allow-hotplug eth0/#allow-hotplug eth0/' -e 's/^iface eth0 inet dhcp/#iface eth0 inet dhcp/' /etc/network/interfaces || true"

# Avoid that last message about the install being complete.
d-i finish-install/reboot_in_progress note
d-i * libraries/restart-without-asking boolean true
